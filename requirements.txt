# Netflix Content Analytics Platform — RUNTIME dependencies
# Exactly what the deployed Streamlit dashboard needs (kept lean so cloud builds
# are fast). Development/analysis extras — tests, notebooks, static charts — live
# in requirements-dev.txt.
# Pinned for reproducibility. Tested on Python 3.12.

# --- Core data stack ---
pandas==2.2.3
numpy==2.1.3

# --- Interactive visualization (dashboard charts) ---
plotly==5.24.1

# --- Machine learning (recommender + search: TF-IDF + cosine) ---
scikit-learn==1.5.2

# --- Dashboard ---
streamlit==1.39.0

# Note: SQLite ships with the Python standard library (module: sqlite3),
# so there is no database driver to install here.
